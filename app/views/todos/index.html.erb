<ul class='list'>
    <% @todos.each do |todo| %>

    <li><%= todo.task %> (<%= todo.done ? "✓" : "✗" %>) <%= link_to "Show", todo %></li>

    <% end %>
    <%= link_to "New task", new_todo_path %>
</ul>