<ul class='list'>
    <% @todos.each do |todo| %>
    <%= link_to todo do %>
    <li><%= todo.task %> <span class='doneIndex'><%= todo.done ? "✔" : "✘" %></span></li>
    <% end %>
    <% end %>
    <button><%= link_to "New task", new_todo_path %></button>
</ul>